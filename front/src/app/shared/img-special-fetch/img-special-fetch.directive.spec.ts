import { ImgSpecialFetchDirective } from './img-special-fetch.directive';

describe('ImgSpecialFetchDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSpecialFetchDirective();
    expect(directive).toBeTruthy();
  });
});
